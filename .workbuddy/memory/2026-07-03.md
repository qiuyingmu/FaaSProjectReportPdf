# 2026-07-03 工作日志

## 宜搭FaaS连接器: 钉钉SDK升级 + DingTalkService重构

### 变更内容
- **pom.xml**: 钉钉SDK从 1.3.38 → 2.2.56，移除未使用的 aliyun-java-sdk-core 4.5.0
- **新建 DingTalkService.java**: Spring @Service 封装钉钉 API（替代老的 DingOpenApiUtil）
  - 支持 accessToken 管理（宜搭默认 + 自定义双模式）
  - 封装了 getOpenUrl / startProcessInstance / searchFormData / saveFormData / batchSaveFormData
  - 使用 SLF4J 日志替代 System.out
  - 与新版 SDK 2.2.56 兼容
- **改造 FaasEntry.java**: 改为 Spring @Component，通过构造器注入 DingTalkService
- **改造 MainApplication.java**: FAAS_ENTRY 从 `new FaasEntry()` 改为构造器注入 (`private final FaasEntry + public MainApplication(FaasEntry)`)
- **删除 DingOpenApiUtil.java**: 已完全被 DingTalkService 替代

### 额外修正
- **MainApplication @Autowired → 构造器注入**: 移除 `@Autowired` 字段注入，改用 `private final` + 单构造器方式（Spring 自动注入）
- **SDK 2.2.56 向后兼容验证**: 通过解包 jar 确认 `com.aliyun.dingtalkyida_1_0` 和 `com.aliyun.dingtalkoauth2_1_0` 包仍然存在，所有旧 import 兼容
- **新版 SDK 额外提供 `dingtalkyida_2_0` 包**: 后续如需新版 yida API 可升级 import 到 `dingtalkyida_2_0`

### 注意事项
- `HttpUtil.java` 和 `YidaConnectorUtil.java` 未改动（与 SDK 升级无关）
- FaasEntry 改为 Component 后，MainApplication 通过 @Autowired 获取 bean 实例
- YidaConnectorUtil.invokeService() 仍然通过 consumeCode 调用宜搭连接器

### 第二次修正：恢复原始文件 + 新建 YidaV2ApiService
- **恢复 FaasEntry.java、MainApplication.java、DingOpenApiUtil.java** 为原始内容，删除我创建的 DingTalkService.java
- **pom.xml** 仅修改 dingtalk 版本为 2.2.56，保留 aliyun-java-sdk-core
- **新建 YidaV2ApiService.java**: 饿汉单例模式，无 Spring 注解，使用 dingtalkyida_2_0 API
  - 饿汉式理由：FaaS 冷启动零延迟、天然线程安全(JVM static final)、构造轻量无副作用
  - 封装了 saveFormData / createOrUpdateFormData / updateFormData / searchFormData / searchFormDataIdList / getFormDataByID / searchFormDataSecondGeneration / startProcessInstance / getInstances / getInstanceById 共 10 个方法
  - 沿用 dingtalkoauth2_1_0 获取 token
  - 使用方式：YidaV2ApiService.INSTANCE.xxx()
