# 2026-07-08 工作日志

## 部署与白屏修复
- **完成事项**：report-springboot 后端 Docker + PostgreSQL 部署到 production，前端通过宝塔 Nginx 子路径 `/report/` 暴露
- **白屏根因**：前后端代码均未适配子路径部署，Vue Router 缺少 base、API 路径硬编码、Nginx 缺少 CORS 头
- **修复清单**：
  - vite.config.js 加入 `base: '/report/'`
  - router/index.js 使用 `import.meta.env.BASE_URL` 作为 createWebHistory 的 base
  - utils/api.js 加入 resolveUrl() 自动加部署前缀
  - Login.vue / Dashboard.vue 所有 fetch 路径加 BASE_URL 前缀
  - nginx-subpath.conf: `alias` → `root`，加 `^~` 避免 regex 覆盖，加 `add_header Access-Control-Allow-Origin '*' always`
- **部署包**：report-deploy.tar.gz（179MB）
